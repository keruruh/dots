# This file is automatically generated by Hellwal. Do not modify it.
# It is launched by Dunst using "dunst -config ~/.cache/hellwal/dunst".

# See dunst(5) for all available configuration options.

[global]
    follow = mouse

    width = (300, 300)
    height = (0, 300)

    origin = top-right
    offset = (20, 20)

    progress_bar = false

    padding = 6
    horizontal_padding = 6
    text_icon_padding = 0

    frame_width = 2
    frame_color = "#%% color4.hex %%"

    # This setting requires a compositor to work properly.
    gap_size = 14

    sort = update

    font = DejaVu Sans Mono 14.0
    markup = full
    format = "<b>%s</b> (%a)\n%b"

    icon_theme = "Adwaita, Papirus"

    stack_duplicates = true
    hide_duplicate_count = false

    # Useful to enforce the timeout set by Dunst configuration.
    # Without this parameter, an application may close the notification before the user
    # defined timeout.
    ignore_dbusclose = true

    mouse_left_click = close_current
    mouse_middle_click = do_action, close_current
    mouse_right_click = close_all

[urgency_low]
    background = "#%% background.hex %%"
    foreground = "#%% foreground.hex %%"

[urgency_normal]
    background = "#%% background.hex %%"
    foreground = "#%% foreground.hex %%"

[urgency_critical]
    background = "#%% background.hex %%"
    foreground = "#%% foreground.hex %%"
